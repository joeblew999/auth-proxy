#!/usr/bin/env bash
#MISE description="Move github pins in skills.toml to upstream HEAD, showing what would change first"
#MISE depends=["build:dev"]
#MISE raw=true
#USAGE arg "[source]" help="One source, or all github sources when omitted"
bin/dev skills bump $usage_source
