#!/usr/bin/env bash
#MISE description="Move github pins in session.toml to upstream HEAD, showing what would change first"
#MISE depends=["build:sessionpin"]
#MISE raw=true
#USAGE arg "[source]" help="One source, or all github sources when omitted"
bin/sessionpin bump $usage_source
