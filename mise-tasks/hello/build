#!/usr/bin/env bash
#MISE description="Build cmd/gui the documented gsx way: vite build, gsx generate, go build (bin/hello-world)"
#MISE depends=["hello:install"]
#MISE dir="cmd/gui"
npm run build
go tool gsx generate -q
go build -o bin/hello-world .
