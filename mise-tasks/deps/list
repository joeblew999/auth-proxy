#!/usr/bin/env bash
#MISE description="List available Go module upgrades in every module, without changing anything"
set -euo pipefail
for dir in . spikes/hello-world; do
  echo "== $dir =="
  (cd "$dir" && go-mod-upgrade --list)
done
