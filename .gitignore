bin
/build/
/grok-oauth-proxy
/dist/
npm/grok-oauth-proxy
npm/grok-oauth-proxy.exe
# wrangler dev local state (KV, cache)
/.wrangler/
# the copy `mise run deploy` runs wrangler on, so its id write-back lands here
/wrangler.deploy.toml
# this clone's Worker URLs, written by `mise run setup:url`
/mise.local.toml
# mise packslip skill links: each developer's `mise skills sync` creates these
# from the active tool versions. Never commit them; .claude/skills/ holds only
# the vendored copies from session.toml.
.claude/skills/.mise-skills.json
.claude/skills/gsx
.claude/skills/gsxui
.claude/skills/hk-configure
.claude/skills/hk-debug
