{
  "mcpServers": {
    "hk": {
      "command": "mise",
      "args": ["exec", "--", "hk", "mcp", "--root", "."]
    },
    "cloudflare-docs": {
      "type": "http",
      "url": "https://docs.mcp.cloudflare.com/mcp"
    }
  }
}
