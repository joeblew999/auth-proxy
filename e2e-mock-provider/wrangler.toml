# Throwaway OpenAI-compatible provider Worker.
#
# Deployed purely so the *deployed* proxy Worker has a publicly reachable
# upstream, which is what closes .plan/any-provider-support.md §6 step 3:
# streaming through Cloudflare, end to end, at no cost.
#
# Delete when done: `mise run e2e_mock_delete`.

name = "mock-openai-provider"
main = "index.js"
compatibility_date = "2026-09-07"
account_id = "7384af54e33b8a54ff240371ea368440"
workers_dev = true

[observability.logs]
enabled = true
head_sampling_rate = 1
invocation_logs = true
